{
  "version": 2,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "buildCommand": "npm run build",
  "installCommand": "npm run build:server && npm install",
  "outputDirectory": "dist"
}
