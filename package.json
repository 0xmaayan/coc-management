{
  "name": "coc-management-server",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/app.js",
  "type": "module",
  "scripts": {
    "build": "rimraf dist && tsc && cp -r contract dist/contract",
    "dev": "npm run build && nodemon app.js",
    "add-build": "git add dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aptos-labs/aptos-cli": "^0.2.0",
    "@aptos-labs/ts-sdk": "^1.27.1",
    "dotenv": "^16.0.3",
    "express": "^4.21.0",
    "tree-kill": "1.2.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "nodemon": "^2.0.22",
    "pre-commit": "^1.2.2",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  }
}
